<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="./css/style.css">
</head>
<body>
    <div class="topnav">
        <a class="active" href="#home">Home</a>
        <a href="#news">News</a>
        <a href="#contact">Contact</a>
        <a href="#about">About</a>
    </div>
    <h2>List users:</h2>

    <table>
    <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Email</th>
        <th>Actions</th>
    </tr>
    <% for (let i = 0; i < dataUser.length; i++) { %>
        <tr>
            <td><%= dataUser[i].firstName %></td>
            <td><%= dataUser[i].lastName %></td>
            <td><%= dataUser[i].email %></td>
            <td>
                <button type="button">
                    <a href="/detail/users/<%= dataUser[i].id %>" target="_blank">Detail</a>
                </button>
                <button type="button">
                    <a href="/user/<%= dataUser[i].id %>" target="_blank">Detail</a>
                </button>
                <button type="button">
                    <a href="/user/<%= dataUser[i].id %>" target="_blank">Detail</a>
                </button>
            </td>
        </tr>
    <% } %>
    
    </table>

    <img src="./images/anhthe.jpg" />
</body>
</html>